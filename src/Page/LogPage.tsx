import { LogsTable } from '~/components/Logs/LogsTable'
import { Layout } from '../Layout/Layout'

export const LogPage = () => {
    return (
        <Layout>
            <LogsTable />
        </Layout>
    )
}
