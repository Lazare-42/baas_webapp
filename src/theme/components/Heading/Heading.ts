import { ComponentStyleConfig } from '@chakra-ui/react'

export const Heading: ComponentStyleConfig = {
    variants: {
        onboarding: {
            fontFamily: 'DM Sans',
            fontWeight: 'light',
        },
    },
}
