import { ComponentStyleConfig } from '@chakra-ui/react'

export const Icon: ComponentStyleConfig = {
    baseStyle: {
        display: 'inline-block', // Convertit l'élément en inline-block
        width: '14px',
        height: '14px',
        margin: 'auto',
        verticalAlign: 'middle', // Centre l'icône dans la boîte, à la fois horizontalement et verticalement
    },
}
